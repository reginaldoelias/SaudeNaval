import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { BebesPage } from './bebes';

@NgModule({
  declarations: [
    BebesPage,
  ],
  imports: [
    IonicPageModule.forChild(BebesPage),
  ],
})
export class BebesPageModule {}
